spring:
  datasource:
    url: jdbc:mysql://localhost/practica
    username: root
    password: ' ' 
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      naming:
        strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
      dialect: org.hibernate.dialect.MySQLDialect 